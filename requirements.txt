# Core runtime
PyQt5
psutil

# Testing
pytest
pytest-qt

# GKM reporting (optional — kiosk runs without this if not configured)
firebase-admin
python-dotenv

# Square Terminal API (optional — falls back to mock if SQUARE_ACCESS_TOKEN not set)
requests

# MDB Pi Hat serial communication (optional — reader disabled if port not found)
pyserial
